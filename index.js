//creating a function that squares a given no

function square(number) {
  return number * number;
}
console.log(square(5));
// Declaring an array and accessing the values inside
let favoriteColors = ["Red ", " Yellow", " Blue", "black"];
console.log(" Jenelle's favorite color is" + favoriteColors[2]);
